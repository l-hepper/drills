#include <iostream>
#include <string>
using namespace std;

int main() {

    // Write a program which takes in a string and turns it into a letter pyramid.
    // E.g. if the user types in "hello"
    // the output should be 
    /*
        h
       h e
      h e l
    h e l l o
    */

    // For an additional challenge continue to layer the pyramid with the text in reverse e.g. 
    /*
        h
       h e
      h e l
    h e l l o
  h e l l o l l
h e l l o l l e h 
    */
}